# FocusFlow Mini App

轻量级待办助手，支持云端样例同步与本地持久化，可直接双击 `index.html` 体验，或在本地启动静态服务器：

```bash
python -m http.server 8000
```

核心能力：
- 使用 fetch 网络请求同步示例任务（JSONPlaceholder）。
- 使用 localStorage 保存任务，刷新不丢失。
- 模板克隆渲染、完成/删除、过滤视图、统计面板。

更多设计与架构说明见 [`report.md`](report.md)。
